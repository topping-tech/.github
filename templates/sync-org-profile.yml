# Runs on push to main in any org repo — updates org profile table directly
name: Sync org profile

on:
  push:
    branches: [main, master]

permissions:
  contents: write

jobs:
  sync:
    uses: topping-tech/.github/.github/workflows/update-org-readme-reusable.yml@main
